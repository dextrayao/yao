# 以後怎麼用？——入口操作介面規劃

> ⚠️ 省錢重點：用 **Claude 對話**分析每張圖都會**燒 token（花錢）**；用 **Mac 上的 Ollama**
> 是跑你自己硬體、**分析幾張都免費、零 token**。所以「量大／長期用」請走入口 B 的免費模式。

兩個入口，依情況挑：
- **入口 A（Claude 對話）**：零安裝、今天就能用，但每張燒 token → 適合偶爾、少量。
- **入口 B（Mac 網頁介面）**：要裝一次，但用本機 Ollama **完全免費** → 適合日常、量大。

---

## 🟢 入口 A：Claude 對話當介面（推薦，日常用這個）

**這就是你要的操作介面**——有拖拉上傳、有送出、看得到結果，而且直接寫進 Notion。零安裝、免金鑰、免碰 Mac。

### 一次性設定（3 分鐘）
1. 打開 Claude（App 或 claude.ai）→ 建立一個 **Project（專案）**，取名「圖像分析助手」
2. 把 Notion 那頁〈圖像逆向分析助手 — 使用說明〉裡的**指令區塊**貼進 Project 的「自訂指令」
3. 完成 ✅

### 每天怎麼用
1. 打開那個 Project
2. **把圖片拖進對話框**（一張或多張都行）
3. 打「分別分析，存進 Notion」
4. 去 Notion 看結果

> 我們今天就是這樣做的——4 張圖已經進你的資料庫。這是最穩、最省事的入口。

**適合**：手邊有圖（截圖、下載的參考圖）想分析建檔。
**限制**：圖要你自己丟；不會自動去 Pinterest 抓。

---

## 🔵 入口 B：Mac Studio 網頁介面（要自動化時再用）

想「自動抓 Pinterest、批量處理」時才需要。裝在你 Mac Studio 上，開一個網頁介面。

### 一次性設定
1. 在 Mac 上把專案抓下來（`git clone` 或下載 zip）
2. Ollama 先裝一個看圖模型：`ollama pull qwen2.5vl:7b`
3. 之後**點兩下 `啟動.command`** 就會自動安裝並開介面（第一次約 3~5 分鐘）

### 每天怎麼用
1. 點兩下 `啟動.command`
2. 瀏覽器自動打開 `http://localhost:8501`
3. 選「手動上傳圖檔」或「Pinterest 網址」→ 按「開始分析」
4. 勾掉「試跑」它就會寫進 Notion

**適合**：要批量、要自動抓 Pinterest、想長期免費用。
**需要**：Mac 上的 Ollama 看圖模型（免費，`ollama pull qwen2.5vl:7b`）。
**費用**：`.env` 的 `ANTHROPIC_API_KEY` **留空 = 免費單模型模式，零 token 成本**；
想要雙模型交叉驗證（品質更高）才填 Claude 金鑰，那會用額度＝花錢。

---

## 怎麼選？

| 你的情況 | 用哪個 |
|---|---|
| 偶爾幾張、懶得裝東西 | 🟢 入口 A（Claude 對話，會燒 token）|
| 常用／量大／想免費 | 🔵 入口 B（Mac 網頁介面 + Ollama，零成本）|
| 想自動抓 Pinterest | 🔵 入口 B |

**一句話**：想省錢就用「入口 B」——裝一次，之後靠 Mac 的 Ollama 免費分析到飽；
入口 A 留給「臨時、手邊沒開 Mac」時應急。
